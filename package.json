{
  "name": "propre-vs-sale",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "npm --prefix server start",
    "server:install": "npm --prefix server install"
  },
  "dependencies": {
    "three": "0.167.0"
  },
  "devDependencies": {
    "vite": "5.4.0"
  }
}
